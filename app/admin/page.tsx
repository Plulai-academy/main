import { Panel } from '@/components/school-admin/ui';

export default function AdminOverviewPage() {
  return (
    <Panel style={{ padding: 32 }}>
      Platform KPIs (DAU, churn, revenue) coming in a later phase — see Schools for now.
    </Panel>
  );
}